let debugIsActive=!1,debugAllowed=!1;function toggleDebug(){if(!debugAllowed)return;let e=document.getElementById("definition");if(debugIsActive=!debugIsActive){document.getElementById("keyboard").setAttribute("border","1");let t=`
width : inner    = ${window.innerWidth}
        client   = ${document.documentElement.clientWidth}
        viewport = ${window.visualViewport.width}
height: inner    = ${window.innerHeight}
        client   = ${document.documentElement.clientHeight}
        viewport = ${window.visualViewport.height}
    `;e.innerHTML=`<pre>${t}</pre>`}else document.getElementById("keyboard").setAttribute("border","0"),e.innerHTML=""}document.addEventListener("DOMContentLoaded",()=>{let e=document.querySelectorAll("img.hidden");for(let t of e)t.classList.remove("hidden");let i=new URLSearchParams(document.location.search);if(GAME.initialize(i),i.has("debug")){debugAllowed=!0;let n=document.getElementById("score");n.addEventListener("click",toggleDebug,!1);let l=document.getElementById("definition");l.innerHTML="Debug is active<br><br>Click on the score to toggle debug ON or OFF"}});